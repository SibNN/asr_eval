# Function get_pipeline (defined in asr_eval/bench/pipelines/_registry.py at lines 123-128)

def get_pipeline(name: str) -> type[asr_eval.bench.pipelines._registry.TranscriberPipeline]:
    """Get a registered pipeline class."""
    ...