# Function compare_pipelines (defined in asr_eval/bench/evaluator.py at lines 418-537)

def compare_pipelines(
    dataset_data: asr_eval.bench.evaluator.DatasetData,
    pipeline_name_1: str,
    pipeline_name_2: str,
) -> asr_eval.bench.evaluator.DatasetPipelinePairComparison:
    """An utility for fine-grained comparison of two pipelines on the
    same dataset.

    To be documented.
    """
    ...