# Function get_dataset_info (defined in asr_eval/bench/datasets/_registry.py at lines 276-282)

def get_dataset_info(name: str) -> asr_eval.bench.datasets._registry.DatasetInfo:
    """Get info for a registered dataset."""
    ...