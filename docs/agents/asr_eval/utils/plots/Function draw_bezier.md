# Function draw_bezier (defined in asr_eval/utils/plots.py at lines 46-74)

def draw_bezier(
    xy_points: list[tuple[float, float]],
    ax: plt.Axes,
    indent: float = 0.1,
    zorder: int = 0,
    lw: float = 1,
    color: str = 'darkgray',
):
    """Draws a Bezier curve."""
    ...