# Function draw_line_with_ticks (defined in asr_eval/utils/plots.py at lines 16-29)

def draw_line_with_ticks(
    x1: float,
    x2: float,
    y: float,
    y_tick_width: float,
    ax: plt.Axes,
    **kwargs: typing.Any,
):
    """Draws a horizontal line with ticks at the ends."""
    ...