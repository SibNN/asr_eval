# Function list_join (defined in asr_eval/utils/misc.py at lines 41-55)

def list_join[T](sep: T, iterable: collections.abc.Iterable[T]) -> list[T]:
    """Combines iterables via a given separator. Acts like str.join,
    but for lists.
    """
    ...