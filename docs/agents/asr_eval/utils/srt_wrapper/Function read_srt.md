# Function read_srt (defined in asr_eval/utils/srt_wrapper.py at lines 30-43)

def read_srt(path: str | pathlib.Path) -> list[asr_eval.segments.segment.TimedText]:
    """Reads .srt transcription file into a list of :code:`TimedText`."""
    ...